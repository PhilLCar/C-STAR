{
  // Adding new extensions to an already supported icon.
  // note: the format must match the existing one. If not, it will use the extension you provide.
  "vsicons.associations.files": [
    { "icon": "csr", "extensions": ["csr"], "format": "svg" }
  ],
  "files.associations": {
    "string_view": "c",
    "diagnostic.h": "c",
    "array": "c",
    "deque": "c",
    "string": "c",
    "unordered_map": "c",
    "vector": "c",
    "initializer_list": "c",
    "str.h": "c",
    "array.h": "c"
  }
}