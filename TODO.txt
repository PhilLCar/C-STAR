- lire le script à Thériault
- duplication of the PARTIAL nodes on recurse can be avoided if deletion handles empty nodes
- PP: if valid =0 delete psr and msr
- PP: C-Mode
- Include "self" keyword to reference own function within itself
- Symbol stream must take into account special types like 0.f or 32Ub
- AST: revert AST on ONE_OF failure too -- to be thought of